<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hfusionhub.mapper.AgentApprovalMapper">

    <resultMap id="BaseResultMap" type="com.hfusionhub.entity.AgentApproval">
        <id column="id" property="id"/>
        <result column="approval_id" property="approvalId"/>
        <result column="task_id" property="taskId"/>
        <result column="run_id" property="runId"/>
        <result column="user_id" property="userId"/>
        <result column="user_role" property="userRole"/>
        <result column="trace_id" property="traceId"/>
        <result column="tool_name" property="toolName"/>
        <result column="risk_level" property="riskLevel"/>
        <result column="tool_input_hash" property="toolInputHash"/>
        <result column="execution_token" property="executionToken"/>
        <result column="execution_token_status" property="executionTokenStatus"/>
        <result column="execution_token_issued_at" property="executionTokenIssuedAt"/>
        <result column="execution_token_consumed_at" property="executionTokenConsumedAt"/>
        <result column="tool_input" property="toolInput"/>
        <result column="arguments_summary" property="argumentsSummary"/>
        <result column="status" property="status"/>
        <result column="decided_by" property="decidedBy"/>
        <result column="decided_at" property="decidedAt"/>
        <result column="reason" property="reason"/>
        <result column="expires_at" property="expiresAt"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, approval_id, task_id, run_id, user_id, user_role, trace_id, tool_name,
        risk_level, tool_input_hash, execution_token, execution_token_status,
        execution_token_issued_at, execution_token_consumed_at, tool_input,
        arguments_summary, status, decided_by, decided_at, reason, expires_at, created_at
    </sql>

    <select id="selectByApprovalId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM agent_approval
        WHERE approval_id = #{approvalId}
    </select>

    <select id="selectByTaskId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM agent_approval
        WHERE task_id = #{taskId}
        ORDER BY created_at DESC
    </select>

    <select id="selectPendingByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM agent_approval
        WHERE user_id = #{userId} AND status = 'pending' AND expires_at > NOW()
        ORDER BY created_at DESC
    </select>

    <select id="selectRecentByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM agent_approval
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <select id="selectExpiredPending" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM agent_approval
        WHERE status = 'pending' AND expires_at &lt;= #{now}
    </select>

    <update id="updateDecision">
        UPDATE agent_approval
        SET status = #{status},
            decided_by = #{decidedBy},
            decided_at = #{decidedAt},
            reason = #{reason}
        WHERE id = #{id}
          AND status = 'pending'
    </update>

    <update id="issueExecutionToken">
        UPDATE agent_approval
        SET execution_token = #{token},
            execution_token_status = #{status},
            execution_token_issued_at = #{issuedAt}
        WHERE id = #{id}
          AND status = 'approved'
          AND execution_token_status = 'none'
    </update>

    <update id="consumeExecutionToken">
        UPDATE agent_approval
        SET execution_token_status = 'consumed',
            execution_token_consumed_at = #{consumedAt}
        WHERE approval_id = #{approvalId}
          AND execution_token = #{token}
          AND execution_token_status = 'issued'
    </update>

    <update id="updateExecutionOutcome">
        UPDATE agent_approval
        SET status = #{status}
        WHERE id = #{id}
          AND status = 'approved'
    </update>

</mapper>
