<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hfusionhub.mapper.PluginMapper">

    <resultMap id="BaseResultMap" type="com.hfusionhub.entity.Plugin">
        <id column="id" property="id"/>
        <result column="plugin_id" property="pluginId"/>
        <result column="name" property="name"/>
        <result column="display_name" property="displayName"/>
        <result column="description" property="description"/>
        <result column="version" property="version"/>
        <result column="author" property="author"/>
        <result column="author_email" property="authorEmail"/>
        <result column="license" property="license"/>
        <result column="min_hfusionhub_version" property="minHfusionhubVersion"/>
        <result column="max_hfusionhub_version" property="maxHfusionhubVersion"/>
        <result column="icon_url" property="iconUrl"/>
        <result column="source" property="source"/>
        <result column="plugin_kind" property="pluginKind"/>
        <result column="status" property="status"/>
        <result column="manifest_hash" property="manifestHash"/>
        <result column="artifact_path" property="artifactPath"/>
        <result column="artifact_hash" property="artifactHash"/>
        <result column="sandbox_config" property="sandboxConfig"/>
        <result column="permissions" property="permissions"/>
        <result column="tool_specs_json" property="toolSpecsJson"/>
        <result column="enabled" property="enabled"/>
        <result column="installed_by" property="installedBy"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="installed_at" property="installedAt"/>
        <result column="canary_weight" property="canaryWeight"/>
        <result column="circuit_open_until" property="circuitOpenUntil"/>
        <result column="previous_version" property="previousVersion"/>
        <result column="health_status" property="healthStatus"/>
        <result column="last_health_check" property="lastHealthCheck"/>
        <result column="sbom_json" property="sbomJson"/>
        <result column="vulnerability_status" property="vulnerabilityStatus"/>
        <result column="last_scan_at" property="lastScanAt"/>
        <result column="container_image" property="containerImage"/>
        <result column="image_digest" property="imageDigest"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, plugin_id, name, display_name, description, version, author, author_email,
        license, min_hfusionhub_version, max_hfusionhub_version, icon_url, source, plugin_kind,
        status, manifest_hash, artifact_path, artifact_hash, sandbox_config,
        permissions, tool_specs_json, enabled, installed_by, tenant_id, installed_at,
        canary_weight, circuit_open_until, previous_version,
        health_status, last_health_check, sbom_json,
        vulnerability_status, last_scan_at, container_image, image_digest,
        created_at, updated_at, deleted
    </sql>

    <select id="selectByPluginId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM plugin
        WHERE plugin_id = #{pluginId} AND deleted = 0
    </select>

    <select id="selectByNameVersion" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM plugin
        WHERE name = #{name} AND version = #{version} AND deleted = 0
    </select>

    <select id="selectByNameVersionAny" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM plugin
        WHERE name = #{name} AND version = #{version}
        LIMIT 1
    </select>

    <select id="selectEnabledPlugins" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM plugin
        WHERE enabled = 1 AND deleted = 0
        ORDER BY name ASC, version DESC
    </select>

    <select id="selectCircuitOpenPlugins" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM plugin
        WHERE status = 'circuit_open'
          AND circuit_open_until IS NOT NULL
          AND circuit_open_until &lt; NOW()
          AND deleted = 0
    </select>

    <update id="updateStatus">
        UPDATE plugin
        SET status = #{status}, updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateEnabled">
        UPDATE plugin
        SET enabled = #{enabled}, updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateArtifact">
        UPDATE plugin
        SET artifact_path = #{artifactPath},
            artifact_hash = #{artifactHash},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateCanary">
        UPDATE plugin
        SET canary_weight = #{canaryWeight},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateHealth">
        UPDATE plugin
        SET health_status = #{healthStatus},
            last_health_check = CURRENT_TIMESTAMP,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateVulnerability">
        UPDATE plugin
        SET vulnerability_status = #{vulnerabilityStatus},
            last_scan_at = CURRENT_TIMESTAMP,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateContainerImage">
        UPDATE plugin
        SET container_image = #{containerImage},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="openCircuitBreaker">
        UPDATE plugin
        SET status = 'circuit_open',
            circuit_open_until = DATE_ADD(NOW(), INTERVAL #{cooldownMinutes} MINUTE),
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="selectVersionsByPluginName" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM plugin
        WHERE name = #{name}
          AND status IN ('active', 'pending')
          AND deleted = 0
        ORDER BY version DESC
    </select>

    <select id="countActiveByUserId" resultType="int">
        SELECT COUNT(*)
        FROM plugin
        WHERE installed_by = #{userId} AND deleted = 0
    </select>

</mapper>
