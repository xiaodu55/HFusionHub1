<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hfusionhub.mapper.TenantMemberMapper">

    <resultMap id="BaseResultMap" type="com.hfusionhub.entity.TenantMember">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="user_id" property="userId"/>
        <result column="role" property="role"/>
        <result column="joined_at" property="joinedAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectByTenantAndUser" resultMap="BaseResultMap">
        SELECT * FROM tenant_member
        WHERE tenant_id = #{tenantId} AND user_id = #{userId} AND deleted = 0
    </select>

    <select id="selectByTenantId" resultMap="BaseResultMap">
        SELECT * FROM tenant_member
        WHERE tenant_id = #{tenantId} AND deleted = 0
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM tenant_member
        WHERE user_id = #{userId} AND deleted = 0
    </select>
</mapper>
