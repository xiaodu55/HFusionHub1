# --- Compiled / Build output ---
*.class
*.jar
*.war
*.nar
*.ear
target/

# --- Package files (sources only, no binaries) ---
*.zip
*.tar.gz
*.rar

# --- Crash / Debug dumps ---
hs_err_pid*
replay_pid*
*.hprof
heapdump*.dump
threaddump*.txt

# --- Logs ---
*.log
logs/

# --- IDE / Editor ---
.idea/
*.iws
*.iml
*.ipr
.vscode/settings.json
*.swp
*.swo
*~

# --- OS junk ---
.DS_Store
Thumbs.db
*.bat
*.cmd

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
coverage.xml
.coverage.*
*.db

# --- Node ---
node_modules/
dist/
.npm

# --- Vue / Vite ---
*.local

# --- Docker ---
docker-compose.override.yml

# --- Uploads ---
uploads/

# --- Claude ---
.claude/
CLAUDE_MEMORY.md
CLAUDE_SESSIONS_MEMORY.md

# --- Project documentation (committed) ---
# docs/ directory is tracked; keep local-only notes in .claude/ or outside the repo

# --- Environment files (contain secrets) ---
.env
.env.local
.env.*.local
docker/.env
java-backend/.env
python-ai/.env

# --- Python AI generated data ---
python-ai/uploads/
python-ai/milvus_data.db/
python-ai/milvus_data.db.active_collection.json
python-ai/milvus_data2.db/
python-ai/milvus_data2.db.active_collection.json
python-ai/milvus_data_test.db/
python-ai/chunks_store.json
python-ai/data/

# --- Python AI evaluation reports (generated; baselines stay committed) ---
python-ai/evaluation/reports/
python-ai/evaluation/*.db
python-ai/evaluation/rag_scoped_graph.json

# --- Python AI test build artifacts ---
python-ai/tests/.tmp-*/

# --- Test / Debug files ---
test_response.json
*_response.json
*.response.json

# --- Runner TLS 私钥（绝不提交；由 scripts/generate-runner-tls.sh 生成） ---
deploy/runner-tls/

# --- Data backups (scripts/backup-data.ps1 output) ---
backup/
