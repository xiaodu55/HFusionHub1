# ============================================================================
# HFusionData Analytics — 分析扩展包(Hadoop 生态)
#
# 独立 profile,不影响主产品交付:
#   精简档: docker compose -f docker-compose.analytics.yml --profile analytics up -d
#   标准档: docker compose -f docker-compose.analytics.yml \
#             --profile analytics --profile analytics-full up -d
#
# 依赖主 compose 的 mysql8(经 external network `docker_default` 连接)。
# 全部 UI 端口仅绑本机回环,不直接对外。
# 端口映射(宿主机): HDFS NN 9870 | YARN RM 18088 | HiveServer2 10000 |
#   Flink UI 8081 | Kafka 29092 | ClickHouse 18123 | Superset 8088
# 内存预算见 docs/BIGDATA_ARCHITECTURE.md(精简档 ≈7.6GB 新增)。
# ============================================================================

name: hfusionhub-analytics

services:

  # ── HDFS ────────────────────────────────────────────────────────────────
  hadoop-namenode:
    image: apache/hadoop:3.3.6
    container_name: hadoop-namenode
    profiles: ["analytics"]
    user: root
    command: hdfs namenode
    environment:
      ENSURE_NAMENODE_DIR: "/hadoop/dfs/name"
      HDFS_NAMENODE_OPTS: "-Xmx768m"
    ports:
      - "127.0.0.1:9870:9870"    # NameNode UI
    volumes:
      - hadoop-namenode:/hadoop/dfs/name
      - ../bigdata/hadoop:/opt/hadoop/etc/hadoop:ro
    networks: [analytics-net]
    healthcheck:
      test: ["CMD-SHELL", "hdfs dfsadmin -report >/dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 10

  hadoop-datanode:
    image: apache/hadoop:3.3.6
    container_name: hadoop-datanode
    profiles: ["analytics"]
    user: root
    command: hdfs datanode
    environment:
      HDFS_DATANODE_OPTS: "-Xmx768m"
    volumes:
      - hadoop-datanode:/hadoop/dfs/data
      - ../bigdata/hadoop:/opt/hadoop/etc/hadoop:ro
    networks: [analytics-net]
    depends_on:
      hadoop-namenode:
        condition: service_healthy

  # ── YARN ────────────────────────────────────────────────────────────────
  yarn-resourcemanager:
    image: apache/hadoop:3.3.6
    container_name: yarn-resourcemanager
    profiles: ["analytics"]
    user: root
    command: yarn resourcemanager
    environment:
      YARN_RESOURCEMANAGER_OPTS: "-Xmx512m"
    ports:
      - "127.0.0.1:18088:8088"   # ResourceManager UI(8088 让给 Superset)
    volumes:
      - ../bigdata/hadoop:/opt/hadoop/etc/hadoop:ro
    networks: [analytics-net]
    depends_on:
      hadoop-namenode:
        condition: service_healthy

  yarn-nodemanager:
    image: apache/hadoop:3.3.6
    container_name: yarn-nodemanager
    profiles: ["analytics"]
    user: root
    command: yarn nodemanager
    environment:
      YARN_NODEMANAGER_OPTS: "-Xmx768m"
    volumes:
      - ../bigdata/hadoop:/opt/hadoop/etc/hadoop:ro
    # 同时接入 core 网络:mysql8 在主 compose 网络,Spark 执行器(运行于本
    # 容器)需经 JDBC 直读 analytics_realtime_metrics / 直写 ADS 镜像。
    networks: [analytics-net, core]
    depends_on:
      - yarn-resourcemanager

  # 运维入口容器:hdfs/beeline/hadoop jar/数据回补都在这里执行
  # (同时接入 core 网络以便 Spark JDBC 直读 mysql8)
  hadoop-client:
    image: apache/hadoop:3.3.6
    container_name: hadoop-client
    profiles: ["analytics"]
    user: root
    command: sleep infinity
    volumes:
      - ../bigdata:/opt/bigdata:ro
      - ../bigdata/hadoop:/opt/hadoop/etc/hadoop:ro
    networks: [analytics-net, core]

  # Spark 提交端(spark-submit on YARN;镜像自带 Hadoop 客户端 jar)
  analytics-spark:
    image: apache/spark:3.5.1
    container_name: analytics-spark
    profiles: ["analytics"]
    user: root
    entrypoint: ["tail", "-f", "/dev/null"]
    environment:
      HADOOP_CONF_DIR: /opt/hadoop/etc/hadoop
      ANALYTICS_DB_USER: hfusionhub
      ANALYTICS_DB_PASSWORD: ${MYSQL_PASSWORD:?请在 .env 提供 MYSQL_PASSWORD}
    volumes:
      - ../bigdata:/opt/bigdata:ro
      - ../bigdata/hadoop:/opt/hadoop/etc/hadoop:ro
    networks: [analytics-net, core]
    depends_on:
      hadoop-namenode:
        condition: service_healthy

  # ── Hive(Metastore 元数据用独立 Postgres;计算存储解耦:外部表指向 Spark
  #    写入的同一批 HDFS Parquet,避免 metastore 客户端版本耦合) ──────────────
  hive-metastore-db:
    image: postgres:16-alpine
    container_name: hive-metastore-db
    profiles: ["analytics"]
    environment:
      POSTGRES_DB: hive_metastore
      POSTGRES_USER: hive
      POSTGRES_PASSWORD: ${HIVE_METASTORE_PASSWORD:-hive123}
    volumes:
      - hive-metastore-db:/var/lib/postgresql/data
    networks: [analytics-net]

  hive-metastore:
    image: apache/hive:4.0.0
    container_name: hive-metastore
    profiles: ["analytics"]
    environment:
      SERVICE_NAME: metastore
      DB_DRIVER: postgres
      SERVICE_OPTS: >-
        -Djavax.jdo.option.ConnectionDriverName=org.postgresql.Driver
        -Djavax.jdo.option.ConnectionURL=jdbc:postgresql://hive-metastore-db:5432/hive_metastore
        -Djavax.jdo.option.ConnectionUserName=hive
        -Djavax.jdo.option.ConnectionPassword=${HIVE_METASTORE_PASSWORD:-hive123}
        -Xmx512m
      HIVE_CUSTOM_CONF_DIR: /opt/hive/conf.custom
    volumes:
      - ../bigdata/hive:/opt/hive/conf.custom:ro
      - ../bigdata/jars/postgresql-42.7.3.jar:/opt/hive/lib/postgresql-42.7.3.jar:ro
    networks: [analytics-net]
    depends_on:
      - hive-metastore-db

  hive-server2:
    image: apache/hive:4.0.0
    container_name: hive-server2
    profiles: ["analytics"]
    environment:
      SERVICE_NAME: hiveserver2
      DB_DRIVER: postgres
      SERVICE_OPTS: >-
        -Djavax.jdo.option.ConnectionDriverName=org.postgresql.Driver
        -Djavax.jdo.option.ConnectionURL=jdbc:postgresql://hive-metastore-db:5432/hive_metastore
        -Djavax.jdo.option.ConnectionUserName=hive
        -Djavax.jdo.option.ConnectionPassword=${HIVE_METASTORE_PASSWORD:-hive123}
        -Xmx768m
      HIVE_CUSTOM_CONF_DIR: /opt/hive/conf.custom
    ports:
      - "127.0.0.1:10000:10000"  # HiveServer2 thrift(Beeline/Superset)
    volumes:
      - ../bigdata/hive:/opt/hive/conf.custom:ro
      - ../bigdata/jars/postgresql-42.7.3.jar:/opt/hive/lib/postgresql-42.7.3.jar:ro
    networks: [analytics-net]
    depends_on:
      - hive-metastore

  # ── Kafka(KRaft,无 ZooKeeper) ─────────────────────────────────────────
  kafka:
    image: apache/kafka:3.7.0
    container_name: analytics-kafka
    profiles: ["analytics"]
    environment:
      KAFKA_NODE_ID: "1"
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: "1"
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: "1"
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: "1"
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "false"
      KAFKA_HEAP_OPTS: "-Xmx512m -Xms256m"
    ports:
      - "127.0.0.1:29092:9092"
    volumes:
      - kafka-data:/bitnami/kafka
    networks: [analytics-net]

  # ── Flink(CDC 增量采集 + 实时聚合;connector jar 安装见 bigdata/flink-jobs/README.md) ──
  flink-jobmanager:
    image: flink:1.18.1
    container_name: flink-jobmanager
    profiles: ["analytics"]
    command: jobmanager
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: flink-jobmanager
        state.checkpoints.dir: file:///opt/flink/checkpoints
        state.backend: hashmap
        execution.checkpointing.interval: 30s
      FLINK_JM_HEAP: 512m
    ports:
      - "127.0.0.1:8081:8081"
    volumes:
      - ../bigdata/jars/flink-sql-connector-kafka-3.1.0-1.18.jar:/opt/flink/lib/flink-sql-connector-kafka-3.1.0-1.18.jar:ro
      - ../bigdata/jars/flink-connector-jdbc-3.1.2-1.18.jar:/opt/flink/lib/flink-connector-jdbc-3.1.2-1.18.jar:ro
      - ../bigdata/jars/flink-sql-connector-mysql-cdc-3.0.1.jar:/opt/flink/lib/flink-sql-connector-mysql-cdc-3.0.1.jar:ro
      - ../bigdata/jars/mysql-connector-j-8.0.33.jar:/opt/flink/lib/mysql-connector-j-8.0.33.jar:ro
      - ../bigdata/flink-jobs:/opt/flink/usrlib/analytics:ro
      - flink-checkpoints:/opt/flink/checkpoints
    networks: [analytics-net, core]

  flink-taskmanager:
    image: flink:1.18.1
    container_name: flink-taskmanager
    profiles: ["analytics"]
    command: taskmanager
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: flink-jobmanager
        taskmanager.numberOfTaskSlots: 8
      FLINK_TM_HEAP: 768m
    volumes:
      - ../bigdata/jars/flink-sql-connector-kafka-3.1.0-1.18.jar:/opt/flink/lib/flink-sql-connector-kafka-3.1.0-1.18.jar:ro
      - ../bigdata/jars/flink-connector-jdbc-3.1.2-1.18.jar:/opt/flink/lib/flink-connector-jdbc-3.1.2-1.18.jar:ro
      - ../bigdata/jars/flink-sql-connector-mysql-cdc-3.0.1.jar:/opt/flink/lib/flink-sql-connector-mysql-cdc-3.0.1.jar:ro
      - ../bigdata/jars/mysql-connector-j-8.0.33.jar:/opt/flink/lib/mysql-connector-j-8.0.33.jar:ro
      - ../bigdata/flink-jobs:/opt/flink/usrlib/analytics:ro
      - flink-checkpoints:/opt/flink/checkpoints
    networks: [analytics-net, core]
    depends_on:
      - flink-jobmanager

  # ── ClickHouse(标准档;精简档可不开,实时指标由 Flink 直写 MySQL) ─────────
  clickhouse:
    image: clickhouse/clickhouse-server:24.3
    container_name: analytics-clickhouse
    profiles: ["analytics-full"]
    environment:
      CLICKHOUSE_DB: analytics
      CLICKHOUSE_USER: analytics
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD:-analytics123}
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: "1"
    ports:
      - "127.0.0.1:18123:8123"
    volumes:
      - clickhouse-data:/var/lib/clickhouse
      - ../bigdata/clickhouse:/docker-entrypoint-initdb.d:ro
    ulimits:
      nofile: 262144
    networks: [analytics-net]

  # ── Superset(BI 报表;首启初始化见 bigdata/superset/README.md) ───────────
  superset:
    image: apache/superset:4.0.1
    container_name: analytics-superset
    profiles: ["analytics"]
    environment:
      SUPERSET_SECRET_KEY: ${SUPERSET_SECRET_KEY:-please-rotate-this-analytics-secret}
      SUPERSET_LOAD_EXAMPLES: "false"
      MAPBOX_API_KEY: ""
    ports:
      - "127.0.0.1:8088:8088"
    volumes:
      - ../bigdata/superset/superset_config.py:/app/pythonpath/superset_config.py:ro
      - superset-home:/app/superset_home
    # 同时接入 core 网络:看板数据源包含 MySQL ADS 镜像(ads_*,V82)
    networks: [analytics-net, core]
    depends_on:
      - hive-server2

  # ── DolphinScheduler 3.2(标准档;日结 DAG 编排/重试/补数 UI) ─────────────
  # standalone 单机档(master/worker/api/alert + H2 元库一体)。worker 挂载
  # docker.sock:任务经 docker exec 复用 analytics-spark 的 spark-submit 路径
  # (与 Java AnalyticsBatchRunner 同一命令模板,dev 单机口径,生产换集群执行器)。
  dolphinscheduler:
    image: apache/dolphinscheduler-standalone-server:3.2.1
    container_name: analytics-dolphinscheduler
    profiles: ["analytics-full"]
    environment:
      # UI 默认账号 admin / dolphinscheduler123,首登改密
      DOLPHINSCHEDULER_OPTS: "-Xms512m -Xmx768m"
    ports:
      - "127.0.0.1:12345:12345"
    volumes:
      - dolphinscheduler-data:/opt/dolphinscheduler/data
      # 静态 docker CLI(bigdata/dolphinscheduler/docker,见 .gitignore;下载方式见
      # bigdata/dolphinscheduler/README.md):任务经 docker exec 复用主栈提交路径
      - ../bigdata/dolphinscheduler/docker:/usr/local/bin/docker:ro
      # 覆盖镜像默认 application.yaml:仅关闭 master/worker 过载保护
      # (宿主机常驻开发栈 CPU 饱和会误触发,master 拒绝消费命令队列)。
      # 注意: H2 保持镜像默认 mem 库 —— 文件库与 DS 3.2.1 的 MySQL 模式 DDL
      # 存在 schema 兼容问题,DS 重启后重跑 bootstrap 脚本重建元数据(约 30s)。
      - ../bigdata/dolphinscheduler/application.yaml:/opt/dolphinscheduler/conf/application.yaml:ro
      - /var/run/docker.sock:/var/run/docker.sock
    ulimits:
      nofile: 65536
    networks: [analytics-net, core]
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:12345/dolphinscheduler/actuator/health >/dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 90s
    deploy:
      resources:
        limits:
          # standalone 四合一 JVM(master+worker+api+alert)堆外开销大,
          # 1.5g 会被 OOM kill(137);3g 为实测下限
          memory: 3g

networks:
  analytics-net:
    name: hfusionhub-analytics
  core:
    external: true
    name: docker_backend      # 主 compose 里 mysql8 所在网络(docker network ls 可查)

volumes:
  hadoop-namenode:
  hadoop-datanode:
  hive-metastore-db:
  kafka-data:
  flink-checkpoints:
  clickhouse-data:
  superset-home:
  dolphinscheduler-data:
